# Invertir colores en zathura
map i recolor

# Zoom out
map _ zoom out

# Dragon: Para mandar el archivo arrastrándolo
map <C-s> exec 'dragon -x "$FILE"'

# Abrir otra isntancia del mismo archivo
map <C-o> exec 'zathura "$FILE"'

# Crear otra copia
map <C-e> exec 'cp "$FILE" copia_nueva.pdf'

# Selection to clipboard
set selection-clipboard clipboard

# Rotate
map R rotate
